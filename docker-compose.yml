version: "3.9"

services:
  ai-resume-analyzer:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ai-resume-analyzer

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped